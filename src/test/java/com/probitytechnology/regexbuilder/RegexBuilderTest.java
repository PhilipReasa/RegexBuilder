import static org.junit.Assert.assertEquals;
import org.junit.Test;
import com.probitytechnology.regexbuilder.RegexBuilder;

public class RegexBuilderTest {
  @Test
  public void running() {
  	RegexBuilder builder = new RegexBuilder();
    assertEquals(1, 1);
  }
}